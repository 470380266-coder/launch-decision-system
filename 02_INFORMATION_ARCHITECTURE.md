# 02_INFORMATION_ARCHITECTURE.md
# V1 操作架构

## 一句话原则
先定义“谁在什么页面操作什么对象”，再做 UI。

## 1. 角色定义
### 管理员
负责：
- 配置基础参数
- 管理单品与 BOM
- 分配共用子料给待生产批次
- 调整生产批次状态
- 回填实际结果
- 校正关键数据

### 采购
只负责维护自己负责的子料跟进事实与到货事实。
负责：
- 更新子料跟进记录
- 确认到货批次
- 填写下单状态、生产状态、预计发货、在途、预计到货、到货数量等

### 运营查看者
只查看顶层结果，不维护底层数据。

## 2. 角色工作台
### 2.1 采购工作台
- 服务角色：采购
- 主工作台目标：维护子料跟进记录与到货确认事实
- 主操作对象：**子料跟进记录**
- 默认看到：自己负责的、未完成的、待跟进记录优先
- 主要动作：更新下单状态、生产状态、预计发货、在途、预计到货、确认到货、填写到货数量
- 不应出现：BOM 编辑、共用料分配、生产批次状态调整、单品结果编辑、实际结果回填
- 采购工作台只显示由备货需求生成的子料跟进记录，不提供采购手工新增子料需求入口。

采购工作台必须区分：
- 系统需求数量 requiredQty：由备货需求和 BOM 自动生成，采购不可修改。
- 实际下单 / 跟进数量：采购根据真实采购情况填写。

如果采购填写的实际下单数量低于 requiredQty：
- 系统应标记为“部分采购”
- 采购必须填写原因
- 管理员端需要能看到该异常

### 2.2 管理员批次工作台
- 服务角色：管理员
- 主工作台目标：处理批次、分配共用料、控制节奏
- 主操作对象：**生产批次 / 共用料分配**
- 默认看到：待生产、受阻、待分配共用料、待回填实际结果的批次
- 主要动作：查看系统自动预估的生产批次，分配共用料，调整批次状态，回填实际结果，校正关键数据

### 2.3 单品管理
- 服务角色：管理员
- 主工作台目标：维护单品与 BOM 主数据
- 主操作对象：**单品 / BOM / BOM 版本**
- 主要动作：新增单品、维护最低开工门槛、标准生产周期、缓冲时间，新增 BOM 版本，启停 BOM

新增动作：
- 发起备货需求
- 输入目标成品数量
- 预览系统按当前 BOM 生成的子料需求
- 确认生成采购跟进记录
- 取消不需要采购跟进的子料

### 2.4 运营结果页
- 服务角色：运营查看者 / 运营使用者
- 主工作台目标：判断当前是否有可排期的备货任务，以及本轮还有多少可分配上架量。
- 主操作对象：ProductLaunchResult / StockingRequest / LaunchAllocation

主要展示：
- 单品状态
- 当前进行中的备货任务
- 本轮可上架量
- 剩余可分配上架量
- 短期新增可上架量
- 下一批预计上架时间
- 关键原因

允许动作：
- 查看结果
- 查看备货需求详情
- 记录本轮上架分配

明确不能做：
- 修改 BOM
- 修改采购跟进事实
- 修改到货批次
- 修改生产批次
- 修改实际生产结果

### 2.5 管理员备货需求页
- 服务角色：管理员
- 主工作台目标：查看备货需求推进状态，判断当前卡在哪个子料、是否已达到齐套门槛
- 主操作对象：StockingRequest / MaterialFollowUp
- 默认看到：已发起但未完成、未生成生产批次、存在缺口或待采购跟进的备货需求
- 主要动作：
  - 查看备货需求列表
  - 查看备货需求详情
  - 查看子料跟进进度
  - 查看已到货数量
  - 查看缺口数量
  - 查看当前最小齐套量
  - 查看是否达到最低开工门槛
- 不应出现：
  - 手工新增采购跟进记录
  - 手工新增生产批次
  - BOM 编辑
  - 共用料分配
  - 批次状态调整

页面定位：
- 备货需求页负责展示“备货推进中”的状态
- 批次工作台只展示已经形成的 ProductionBatch
- 单品管理只负责维护单品和 BOM 主数据

## 3. 页面级信息架构
一级导航建议：
- 单品结果
- 备货需求
- 批次工作台
- 单品管理
- 系统配置（仅管理员）

## 4. 对象归属原则
- 采购跟进记录：采购工作台主对象
- 到货批次：采购确认到货后的事实对象
- 共用料分配：管理员批次工作台动作
- 生产批次：系统自动预估对象，由管理员调度状态
- 实际结果：挂在批次详情里
- BOM：低频主数据，放在单品管理

### 5. 管理员备货需求页
- 服务角色：管理员
- 主工作台目标：查看每一轮备货需求从发起、采购跟进、到货、生产、可上架、分配完结的推进状态。
- 主操作对象：StockingRequest / MaterialFollowUp / ProductionBatch / LaunchAllocation
- 默认看到：进行中、未分配完、存在缺口、未生成批次或待上架分配的备货需求。

管理员备货需求页需要展示以下关键字段：
- 目标备货量
- 本轮可上架量
- 已分配上架量
- 剩余可分配上架量
- 目标缺口
- 当前状态
- 关键缺口
- 是否允许正常完结
- 是否需要管理员终止本轮

主要动作：
- 查看备货需求列表
- 查看备货需求详情
- 查看子料跟进进度
- 查看当前最小齐套量
- 查看是否达到最低开工门槛
- 查看本轮可上架量
- 查看剩余可分配上架量
- 记录本轮上架分配
- 将剩余可分配量为 0 的备货任务标记为已分配完 / 已完结

页面定位：
- 备货需求页负责展示“本轮备货推进状态”。
- 批次工作台只展示已经形成的 ProductionBatch。
- 单品管理只维护 Product / BOM 主数据。
- 单品结果页面向运营展示可排品判断，不承担长期库存展示。

页面必须区分：
- “本轮可上架量已分配完”
- “本轮备货目标已达成”
- “本轮备货任务已完结”

禁止：
- 将剩余可分配上架量为 0 直接显示为“已完结”
- 在目标未达成时隐藏缺口

## 6. 角色动作边界
| 角色 | 主工作台 | 主对象 | 可做动作 | 明确不能做 |
|---|---|---|---|---|
| 采购 | 采购工作台 | 子料跟进记录 | 更新进度、确认到货 | BOM、分配、批次调度、结果改写 |
| 管理员 | 批次工作台 / 单品管理 | 生产批次、共用料、单品/BOM | 分配、调状态、回填结果、维护主数据 | 不应把采购录入当主视图 |
| 运营 | 结果页 | 单品结果 | 查看、筛选、追原因 | 任何底层编辑动作 |
