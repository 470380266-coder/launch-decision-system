# 03_WORKFLOW.md
# V1 业务流程

## 一句话原则
先定义对象怎么流转，再定义页面怎么长。

## 1. 总流程概览

**单品 → BOM 当前生效版本 → 备货需求 → 子料跟进记录 → 到货确认 → 到货批次 → 共用料分配 → 生产批次 → 实际结果回填 → 本轮可上架量 → 上架分配 → 备货任务完结**


## 2. 各阶段说明
### 2.1 单品定义阶段
输入对象：
- 单品
- 单品基础配置
关键动作：
- 创建单品
- 配置最低开工门槛
- 配置标准生产周期
- 配置缓冲时间

### 2.2 BOM 定义阶段
输入对象：
- 单品
- BOM 版本
- BOM 项
关键动作：
- 新建 BOM 版本
- 配置子料构成与单耗
- 启用当前生效 BOM 版本

### 2.3 备货需求阶段

管理员发起备货需求。

输入对象：
- 单品
- 当前生效 BOM
- 目标成品数量

关键动作：
- 选择单品
- 输入计划备货成品数量
- 系统读取当前生效 BOM
- 系统按 BOM 子料单耗自动计算子料需求数量
- 管理员确认哪些子料需要生成采购跟进记录
- 管理员可取消不需要下单的子料
- 管理员确认后，系统生成子料跟进记录

输出：
- 备货需求
- 待采购跟进的子料跟进记录

### 2.3 子料跟进阶段
采购围绕**子料跟进记录**更新：
- 下单状态
- 生产状态
- 预计发货时间
- 在途状态
- 预计到货时间
- 跟进备注
- 下次跟进时间

### 2.4 子料跟进阶段

采购围绕系统生成的**子料跟进记录**更新进度。

采购不手工创建子料跟进记录，只负责跟进已经生成的记录。

采购可更新：
- 下单状态
- 生产状态
- 预计发货时间
- 在途状态
- 预计到货时间
- 跟进备注
- 下次跟进时间

采购不能修改系统生成的子料需求数量 requiredQty。

采购只能填写：
- 实际下单数量
- 实际到货数量
- 预计发货时间
- 预计到货时间
- 跟进状态
- 跟进备注

如果实际下单数量低于 requiredQty，系统应标记为“部分采购”，并要求采购填写原因。

部分采购不阻断流程，但必须在管理员备货需求页显性展示，避免备货目标缺口被隐藏。

### 2.5 共用料分配阶段
- 非共用子料：系统自动归属到对应单品 / 待生产批次
- 共用料：管理员手动分配给待生产批次
核心规则：未分配的共用料不得参与预测

### 2.6 生产批次形成阶段
系统自动计算：
- 当前最小齐套可生产量
- 是否达到单品最低开工门槛
若达到：
- 自动形成生产批次
若未达到：
- 继续等待后续到货 / 分配

### 2.7 生产批次调度阶段
管理员可以调整批次状态：
- 待生产
- 暂缓
- 已完成

### 2.8 单品结果生成阶段

系统在关键业务数据发生变化后，实时重新计算单品结果。

系统不计算单品长期库存，也不追踪每日销量扣减。

系统围绕当前进行中的备货需求输出：
- 本轮可上架量
- 剩余可分配上架量
- 短期新增可上架量
- 下一批预计上架时间
- 单品状态
- 关键原因

说明：
- 本轮可上架量来自实际结果回填。
- 剩余可分配上架量来自本轮可上架量减去已分配上架量。
- 当本轮剩余可分配上架量为 0 时，本轮备货任务可以完结。
- 下一次需要继续上架该单品时，应重新发起新的备货需求。

### 2.9 实际结果回填阶段
管理员回填：
- 实际开工时间
- 实际完成时间
- 实际上架时间
- 实际上架数量

### 2.10 上架分配阶段

运营或管理员根据本轮可上架量进行上架分配。

输入对象：
- 备货需求
- 实际结果
- 本轮可上架量

关键动作：
- 查看本轮可上架量
- 记录分配数量
- 记录分配去向，如直播间、活动、渠道或备注
- 系统计算剩余可分配上架量

输出：
- 上架分配记录
- 剩余可分配上架量

### 2.11 备货任务完结阶段

备货任务完结必须同时判断两个口径：

1. 上架分配口径
- 本轮可上架量是否已经全部分配完。
- remainingAllocatableQty = roundLaunchQty - allocatedLaunchQty。

2. 备货目标口径
- 本轮实际形成的可上架量是否达到目标备货量。
- targetGapQty = targetFinishedQty - roundLaunchQty。

正常完结条件：
- roundLaunchQty >= targetFinishedQty
- remainingAllocatableQty = 0

如果：
- remainingAllocatableQty = 0
- 但 roundLaunchQty < targetFinishedQty

则不得自动完结，只能显示为：
- 已分配完但目标未达成

此时系统必须展示：
- 目标备货量
- 本轮可上架量
- 已分配上架量
- 目标缺口
- 关键缺口原因

管理员可以选择“终止本轮备货”。

管理员终止本轮时，必须填写终止原因。终止后状态为：
- 短缺完结 / 部分完结

完结后的备货任务不再承担后续运营排品指导作用。
如果该单品后续需要继续备货，应重新发起新的备货需求。

## 3. 对象分层说明
- 主对象：单品
- 结构对象：BOM、BOM 版本、BOM 项
- 过程对象：子料跟进记录
- 事实对象：到货批次、实际结果
- 决策对象：生产批次、单品结果

## 4. 谁停留在哪一段流程
- 采购：子料跟进记录、到货确认、到货批次形成
- 管理员：共用料分配、生产批次调度、实际结果回填、单品/BOM 配置
- 运营：单品结果查看、单品详情查看

## 5. 核心纪律
- 先有跟进，再有到货批次
- 先分配，再预测
- 先有批次，再有结果
- 先有预测，再有实际校验
