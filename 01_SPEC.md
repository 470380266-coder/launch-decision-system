# 01_SPEC.md
# V1 产品规范：分批到料驱动的单品上架决策系统

## 一句话版本
一个面向直播间运营的上架决策系统，基于子料到货批次、共用料人工分配、BOM 当前生效版本、单品最低开工门槛和标准生产周期，自动预估单品生产批次，并输出“当前已可上架数量、短期新增可上架量和单品状态”，帮助团队明确知道：几号能上什么品，上多少个。

## 1. 产品定位
这不是一个通用后台，也不是一个完整供应链系统。
它的定位是：**一个基于分批到料、分批生产和规则计算的单品上架决策系统。**

## 2. Problem Statement（问题陈述）
### 2.1 现有判断方式过于静态
过去通常是一个单品对应一组子料件，每个子料件只跟一个整体进度，用单一预计时间去判断整个单品能否上架。

### 2.2 真实业务是分批流动的
实际业务中：
- 子料件经常分批到货
- 同一子料件可能被多个单品共用
- 到货后需要先分配给待生产任务
- 单品不是等所有料一次性全齐后再统一上架，而是可以随着生产推进，逐步形成可上架结果

### 2.3 运营缺少可信的决策结果
运营真正需要知道的是：
- 这个单品现在能不能上
- 现在能上多少
- 接下来短期内还能新增多少
- 当前为什么受阻

当前方式无法稳定回答：**几号能上什么品，上多少个。**

### 2.4 人工脑算成本高且容易失真
当前判断依赖项目管理、采购、运营之间频繁口头确认，容易出现：
- 同一批料被重复乐观计算
- 共用料归属不清
- 生产周期未被纳入
- 预计时间和数量失真

因此，V1 的核心问题是：
> **如何在分批到料、共用料人工分配、分批生产的真实业务前提下，自动给出单品级的可信上架决策结果。**

## 3. 核心用户
### 第一核心用户：直播间运营
运营主要看：
- 当前已可上架数量
- 短期新增可上架量
- 下一批预计上架时间
- 单品状态
- 关键原因

### 第二层用户：采购
采购负责更新子料跟进事实和到货事实。

### 第三层用户：管理员
管理员兼项目管理调度职责，负责：
- 配置基础参数
- 分配共用子料
- 调整生产批次状态
- 校正关键数据
- 管理单品/BOM

## 4. Proposed Solution（方案描述）
构建一个小规模内部使用的网页系统。
系统围绕“长期存在的单品对象”工作。
每个单品可持续补货、持续生产、持续上新。
系统底层按**生产批次**推演，顶层按**单品级**输出结果。

### 4.1 核心输出补充：备货目标达成口径

系统必须区分两个概念：

1. **上架分配是否完成**
   - 判断本轮已经形成的可上架量是否已经被运营分配完。
   - 剩余可分配上架量 = 本轮可上架量 - 已分配上架量。

2. **备货目标是否达成**
   - 判断本轮实际形成的可上架量是否达到管理员最初填写的目标备货量。
   - 目标缺口 = 目标备货量 - 本轮可上架量。

重要原则：
- 剩余可分配上架量 = 0，不等于备货任务完成。
- 只有当本轮可上架量 ≥ 目标备货量，并且剩余可分配上架量 = 0 时，备货任务才可以正常完结。
- 如果本轮可上架量 < 目标备货量，但剩余可分配上架量 = 0，系统应显示“已分配完但目标未达成”，并展示目标缺口。

### 4.2 核心计算单位
系统底层按**生产批次**计算，而不是按单品整体是否齐套计算。

### 4.3 自动成批逻辑
- 先计算当前最小齐套可生产量
- 再对照该单品配置的最低开工门槛
- 达到门槛，才自动形成生产批次
- 未达到门槛，则继续等待

### 4.4 共用子料处理
- 非共用子料：自动归属到对应单品/待生产批次
- 共用子料：必须由管理员手动分配到待生产批次
- 未分配前不参与预测
- 系统不自动抢料

### 4.5 预计上架时间逻辑
- 批次满足开工条件
- 加上单品标准生产周期
- 再加缓冲时间
- 得到预计上架时间
V1 不计算真实产能冲突与复杂排期。

### 4.6 实际结果回填
至少记录：
- 实际开工时间
- 实际完成时间
- 实际上架时间
- 实际上架数量

## 5. Technical Constraints（技术约束）
- 网页系统
- 公网可访问
- 通过账号登录控制访问
- 电脑端为主，手机端只读
- 小规模内部系统，低并发，中等数据量
- 基于规则算法，不接 Agent
- 每 1 小时批量重算
- 系统自动预估生产批次
- 支持 BOM 版本，但同一时点只允许一个当前生效版本参与预测
- 必须精确记录子料到货批次
- 共用料必须先分配，未分配不参与预测

## 6. Non-goals（明确不做的事）
- 不做完整供应链系统
- 不做自动抢料
- 不做复杂排产
- 不做 Agent 主导预测
- 不做活动模块
- 不做多直播间 / 多渠道精细分货
- 不做锁货 / 销售 / 消耗管理
- 不做人工重组批次
- 不做异常处理闭环
- 不做主动消息推送
- 不做仪表盘
- 不做批量导入 / 导出
- 不做长期库存管理
- 不做每日销量扣减
- 不接销售订单系统
- 不做订单级库存扣减
- 不计算单品长期剩余可售库存
- 不用历史累计已上架量代表当前库存余量
- 不允许将“本轮已分配完”直接等同于“备货任务已完成”
- 不允许隐藏备货目标缺口
- 不允许采购静默将系统需求数量改小

## 7. Success Criteria（成功标准）
- 预计上架时间偏差：±2 天
- 预计上架数量偏差：±10%
- 运营能够基于系统结果直接做排 / 等 / 换判断
- 项目管理、采购、运营围绕同一套结果工作
- 系统形成“预测结果—实际结果—偏差校验”的闭环
